## Nested conflict review area

This folder contains files preserved from the old nested `SmileMedical/SmileMedical` tree
after exact-hash duplicates were removed.

- `different_hash_same_relpath`: files that also exist in `SmileMedical/` but with different content.
- `review_only_files`: files that only existed in the nested tree.

Use `docs/hash_analysis_post_cleanup_20260306.json` as the source of truth before deciding
which files should be merged into the main `SmileMedical/` tree.
